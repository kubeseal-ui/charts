apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeseal-ui.uiName" . }}
  labels:
{{ include "kubeseal-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
{{ include "kubeseal-ui.ui.selectorLabels" . | nindent 4 }}
